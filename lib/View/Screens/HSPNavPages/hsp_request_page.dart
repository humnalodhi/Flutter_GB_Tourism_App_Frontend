import 'package:flutter/material.dart';

class HSPRequestPage extends StatelessWidget {
  const HSPRequestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text("Requests Page"),
      ),
    );
  }
}
