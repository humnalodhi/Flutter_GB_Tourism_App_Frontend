import 'package:flutter/material.dart';

class HSPChatPage extends StatelessWidget {
  const HSPChatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Chat Page"),
      ),
    );
  }
}
