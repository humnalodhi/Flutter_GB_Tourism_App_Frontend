import 'package:flutter/material.dart';

class CSPMainPage extends StatelessWidget {
  const CSPMainPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
