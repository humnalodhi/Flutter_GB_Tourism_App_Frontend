import 'package:flutter/material.dart';

class CSPRequestPage extends StatelessWidget {
  const CSPRequestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
